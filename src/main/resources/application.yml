server:
  servlet:
    contextPath: /api/
jasypt:
  encryptor:
    bean: jasyptStringEncryptor
spring:
  main:
    allow-bean-definition-overriding: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
---
spring:
  profiles:
    active: local
    include: loauth
  config:
    import: optional:configserver:https://dev.yeohaengparty.com/config
#        configserver: http://localhost:8888



---

spring:
  profiles:
    active: prod
    include: poauth
---

spring:
  profiles:
    active: dev
    include: doauth