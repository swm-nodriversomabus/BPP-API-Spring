server:
  servlet:
    contextPath: /api/

---
spring:
  profiles:
    active: local
    
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Yeohangparty?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: (nodriversomabus)
    
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
---

spring:
  profiles:
    active: prod
---

spring:
  profiles:
    active: dev